export const fieldClass =
    "w-full rounded-xl border border-line bg-white px-3.5 py-2.5 text-sm text-ink placeholder:text-muted focus:outline-none focus:ring-2 focus:ring-primary/40 focus:border-primary transition";

/** Same as `fieldClass`, with room on the right for the show/hide button. */
export const passwordFieldClass = `${fieldClass} pr-11`;
